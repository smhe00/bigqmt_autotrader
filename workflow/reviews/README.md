# Reviews

Architect independent reviews only.

每个 review 必须与 task/report 使用相同的 `task_key`：

```text
P6-T001-I01__architect-review.md
```

PASS / CHANGES_REQUIRED / BLOCKED 等 Gate 结论在这里记录；长期项目状态可再同步到 `docs/`。
